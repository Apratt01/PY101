numbers = [1, 2, 3]
numbers[2] = 5

print(numbers)

# numbers[2] reassigns the element at index 2 to the integer 5.
# numbers[6] = 5 will raise an error because index 5 does not exist in the list
# and you cannot use index syntax to add an element to a list.